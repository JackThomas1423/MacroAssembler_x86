[BITS 64]

section .text

global _start

_start:
    push reverse "Test" as BYTE
    mem 4 as BYTE
    dup as QWORD

    pop rbx
    mov BYTE [rbx],   'a'
    mov BYTE [rbx+1], 'b'
    mov BYTE [rbx+2], 'c'
    mov BYTE [rbx+3], 'd'

    print 4 as PTR
    print 4
    exit 0